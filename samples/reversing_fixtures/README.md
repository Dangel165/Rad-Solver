# Reversing Fixture Samples

These are tiny synthetic reversing-style fixtures used by tests. They are not real malware or exploit binaries.

The automated tests build the byte fixtures in memory so the repository stays small and easy to inspect.
